import { discountsInPerCentage } from "./constants";
import {
  getAllBrandsFromProducts,
  sortByName,
  priceOfCart,
  pluralize,
} from "./utils";

export {
  discountsInPerCentage,
  getAllBrandsFromProducts,
  sortByName,
  priceOfCart,
  pluralize,
};
